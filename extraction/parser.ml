(* Planned, unverified surface front end.

   The future parser will handle explicit Church F and the HM fragment,
   with source positions and rejection of trailing input. Resolve term
   names and type names in separate environments before F.Check.

   The current demo uses the shared Rocq examples. No parser is provided
   by this scaffold. *)
