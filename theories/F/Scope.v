(** * Type-variable scope — planned

    Define [closed n T], prove preservation by [type_lift] and [type_subst].
    In particular, substituting a type scoped at n into a body scoped at
    S n must produce a type scoped at n.

    No definitions or proofs are supplied by this scaffold. *)

From SystemF.F Require Import Syntax.
